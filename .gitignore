# Build output
target/
build/
out/
*.jar
*.war
*.ear
*.class
**/column-types.json

# IDE
.idea/
*.iml
*.iws
*.ipr
.vscode/
.project
.classpath
.settings/
.factorypath

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Maven
.mvn/wrapper/maven-wrapper.jar

# Local env / secrets
.env
.env.local
*.pem
*.key
# Salida de scripts/gen-jwt-keys.sh — el par RSA que firma los JWT.
secrets/

# Node / Vite (admin-ui/)
node_modules/
dist/
dist-ssr/
coverage/
.vite/
.cache/
playwright-report/
test-results/
# TypeScript build artifacts (admin-ui)
*.tsbuildinfo
*.d.ts
# Built SPA bundled into the gateway jar at dev time. The real
# artifact is the admin-ui/dist/ output, NOT what's copied into the
# gateway's classpath; serving from inside the jar is a debugging
# fallback only.
api-gateway/src/main/resources/static/admin/

# Agents
skills-lock*
.agents/
.playwright-mcp/

# .claude/ y .agents/ se ignoran completos, EXCEPTO:
#   - los subagentes del repo (.claude/agents/)
#   - un conjunto explícito de skills (su fuente: SKILL.md + recursos)
.claude/*
!.claude/agents/
!.claude/skills/
.claude/skills/*
# Skills externas versionadas (ver skills-lock.json)
!.claude/skills/flyway-migrations/
!.claude/skills/plpgsql/
!.claude/skills/postgresql/
!.claude/skills/postman-collection-generator/
!.claude/skills/java-spring-boot/
!.claude/skills/java-springboot/
!.claude/skills/spring-cloud-basics/
!.claude/skills/docker-compose-orchestration/
!.claude/skills/github-actions/
!.claude/skills/reviewing-oracle-to-postgres-migration/
# Skills propias del repo
!.claude/skills/next-migration-number/
!.claude/skills/server-status/
!.claude/skills/new-query-endpoint/


